package edu.eci.arep.mathServices.model;

public class Calculator {
	
	public static double calculateSqrt(double value) {
		return Math.sqrt(value);
	}
	
	public static double calculateExp(double value) {
		return Math.pow(Math.E, value);
	}
}